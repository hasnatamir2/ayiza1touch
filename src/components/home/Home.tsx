import React from "react";
import { Container, Card } from "react-bootstrap";

const Home = (props: any) => {
    const { values } = props;
    return <Container>
        <Card>

        </Card>
    </Container>;
}

export default Home;